# Vite Environment Variables Example

# Desabilita cache em produção (opcional, já configurado em vite.config.ts)
VITE_DISABLE_CACHE=true

# Force rebuild admin dashboard
VITE_FORCE_REBUILD_ADMIN=true

# Supabase (se aplicável)
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Admin Panel Security (OPCIONAL - Use Supabase Auth ao invés disso)
# ATENÇÃO: Mude esta senha para algo seguro! Não use a senha padrão em produção!
VITE_ADMIN_MASTER_PASSWORD=CHANGE_THIS_PASSWORD_NOW
